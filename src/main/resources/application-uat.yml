spring:
  application:
    name: export-service

filesystem:
  tmp-folder: /tmp/export-service/uat/tmp
  export-folder: /tmp/export-service/uat/export

scheduling:
  redemption-cron: "0 0 * * * *"  # Every hour
  outpay-cron: "0 0 */6 * * *"     # Every 6 hours
  own-and-ben-cron: "0 0 */12 * * *" # Every 12 hours
